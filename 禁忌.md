以下直接给出两份核心说明，可直接用于答辩或交接。

---

# 一、CMeKG 导入核心方法

## 总体链路

```
Neo4j dump → 本地恢复 → cmekgEtl.js 抽取 → JSON/JSONL → 云数据库 cme_kg_medicines
```

## 步骤 1：准备环境

- dump 文件格式：Neo4j 5.x `neo4j-admin database dump`（魔数 `DZV1` + zstd）
- 需要 **Neo4j 5.x + JDK 17+**
- dump 不纳入 git，仅提交 ETL 生成的 JSON

## 步骤 2：恢复本地图库

```bash
neo4j stop
neo4j-admin database load cmekg --from-path=./dumps --overwrite-destination=true
neo4j start
```

## 步骤 3：探查 schema 并校准 ETL

CMeKG v5.2 实测结构：

| 项目 | 值 |
|------|-----|
| 药品节点 | `Drug`（属性 `name`） |
| 禁忌关系 | `contraindications` → `Disease` / `Complication` / `Symptom` |
| 注意事项 | `precautions` → `Precautions`（可解析过敏/人群） |
| 药物相互作用 | 图谱中**无** Drug-Drug 边，需从种子数据合并 |

```bash
cd SRTP/scripts
npm install
$env:NEO4J_PASSWORD="你的密码"
$env:CMEKG_DATABASE="cmekg"
node cmekgEtl.js --probe   # 探查标签/关系，校准 CONFIG
```

## 步骤 4：抽取生成 JSON

```bash
node cmekgEtl.js
# 输出 SRTP/scripts/cmekg_seed_data.generated.json
```

每条记录结构（与种子数据一致）：

```json
{
  "name": "阿司匹林肠溶片",
  "aliases": ["阿司匹林", "Aspirin"],
  "category": "解热镇痛抗炎药",
  "contraindications": {
    "diseases": ["胃溃疡", "消化道出血"],
    "allergies": ["阿司匹林", "水杨酸类"],
    "populations": ["孕妇"]
  },
  "interactions": [
    { "target_drug": "华法林", "risk_level": "高", "mechanism": "...", "advice": "..." }
  ]
}
```

## 步骤 5：导入云数据库

微信云开发不接受裸 JSON 数组，使用：

- `cmekg_import_ready.json`（`{ "medicines": [...] }` 包装，推荐）
- 或 `cmekg_import_ready.jsonl`（每行一条）

在云开发控制台 → 集合 `cme_kg_medicines` → 导入。

索引：`name` 唯一索引，`aliases` 普通索引。

## 步骤 6：验证

- 导入后无需改 `checkMedicineContraindication` 云函数
- 在小程序添加已知药品（如阿司匹林），填写胃溃疡病史，应弹出高危预警

---

# 二、禁忌分析功能实现关键步骤与方法

## 总体架构

```
用户健康档案(本地) + 当前用药列表
        ↓
前端 manage.js 调用云函数
        ↓
checkMedicineContraindication（图谱检索）
        ↓
analyzer.js（三类风险碰撞）
        ↓
返回 warnings → 前端弹窗 → 用户确认后入库
```

## 步骤 1：准备知识图谱数据

云数据库集合 `cme_kg_medicines`，每条药品文档需包含：

| 字段 | 用途 |
|------|------|
| `name` | 主名称，检索用 |
| `aliases` | 别名数组，辅助匹配 |
| `contraindications.diseases` | 疾病禁忌列表 |
| `contraindications.allergies` | 过敏禁忌列表 |
| `interactions[].target_drug` | 相互作用目标药 |
| `interactions[].risk_level` | 高/中/低 |
| `interactions[].mechanism` | 作用机制 |
| `interactions[].advice` | 用药建议 |

当前已有 50 种种子数据（`cmekg_seed_data.json`），全量扩充走上方 ETL 流程。

## 步骤 2：采集用户健康画像

用户在 `packageA/pages/profile-edit` 填写，存入本地 `healthProfile`：

| 字段 | 对应分析输入 |
|------|-------------|
| `allergies` | → `userAllergies` |
| `chronicDiseases` | → `userDiseases` |
| `specialStatusName`（如"孕妇"） | 合并进 `userDiseases` |
| `surgeries` | 当前未参与碰撞，可扩展 |

同步写入云端 `health_profiles` 集合，供跨端使用。

## 步骤 3：前端触发时机（`manage.js`）

用户点击「添加药品」→ 基础表单校验通过后：

1. 从 `medicineList` 提取 `currentMedicines`（已添加药品名列表）
2. 从 `healthProfile` 组装 `userDiseases`、`userAllergies`
3. 调用云函数 `checkMedicineContraindication`，传入：
   - `newMedicineName`：待添加药品名
   - `currentMedicines`
   - `userDiseases`
   - `userAllergies`

## 步骤 4：云函数图谱检索（`index.js`）

在 `cme_kg_medicines` 中查找新药：

```javascript
// 按 name 正则（不区分大小写）或 aliases 精确匹配
db.collection('cme_kg_medicines').where(_.or([
  { name: db.RegExp({ regexp: newMedicineName, options: 'i' }) },
  { aliases: newMedicineName }
])).get()
```

- **找到**：取第一条，进入风险研判
- **未找到**：返回 `code: 0`，提示"图谱库中暂无该药品数据，请遵医嘱服用"，**不阻断添加**

## 步骤 5：核心匹配算法（`analyzer.js`）

纯函数 `analyzeContraindications(kgData, profile)`，三类碰撞，均采用**双向子串匹配**（`a.includes(b) || b.includes(a)`）：

### 碰撞 1：个人病史禁忌

```
图谱 contraindications.diseases  ×  用户 userDiseases
```

命中 → 产出 `type: 'disease'`，`level: '高危'`

例：用户填"严重胃溃疡"，图谱有"胃溃疡" → 命中

### 碰撞 2：过敏史

```
图谱 contraindications.allergies  ×  用户 userAllergies
```

命中 → 产出 `type: 'allergy'`，`level: '高危'`

### 碰撞 3：同服药物相互作用

```
图谱 interactions[].target_drug  ×  用户 currentMedicines
```

命中 → 产出 `type: 'interaction'`
- `risk_level === '高'` → `level: '高危'`
- 其他 → `level: '中风险'`

每条 warning 含 `title`、`detail`（含机制与建议），供前端直接展示。

## 步骤 6：前端展示与用户决策

`manage.js` 收到 `hasWarning: true` 时：

1. 拼接所有 warning 为弹窗内容
2. `wx.showModal` 标题"用药安全预警"
3. 用户选「取消」→ 中止添加
4. 用户选「执意添加」→ 调用 `executeAddMedicine` 写入本地 + 云端

无警告则直接入库。

## 步骤 7：独立禁忌查询页（`contraindication`）

`packageA/pages/contraindication` 提供主动查询入口，调用 `searchContraindication` 云函数按关键词搜索图谱，并支持用户维护个人禁忌列表（`myContraindications` 本地存储）。

## 扩展新药品（无需改代码）

1. 在 `cmekg_seed_data.json` 或 ETL 输出中新增药品记录
2. 导入 `cme_kg_medicines`
3. 自动生效

## 验证方法

| 类型 | 方法 |
|------|------|
| 单元测试 | `SRTP/tests/analyzer.test.js`，7 个用例覆盖三类碰撞 |
| 手动测试 | 见 `test-cases.md` 第 3 节（CON-001~INT-003） |
| 端到端 | 健康档案填"胃溃疡" → 添加"阿司匹林" → 应弹高危预警 |

---

**一句话总结**：CMeKG 导入走「Neo4j 恢复 → ETL 抽取 → 云库导入」；禁忌分析走「健康档案 + 用药列表 → 云函数查图谱 → analyzer 三类子串碰撞 → 前端弹窗确认」，数据扩充只改 JSON，不改代码。